# Training data

Place approved asset-state-time training data here as `asset_states.csv`.
Do not commit restricted inspection records, personal data or large datasets.
Use controlled object storage and record an immutable dataset checksum instead.

